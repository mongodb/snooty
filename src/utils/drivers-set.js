/**
 * This set corresponds to the repo branch projects that have an entry
 * in the search filter under category.
 */
export const driversSet = new Set(['golang', 'ruby', 'php', 'csharp', 'java', 'node']);
