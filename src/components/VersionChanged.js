import React from 'react';
import VersionModified from './VersionModified';

const VersionChanged = props => <VersionModified {...props} introText="Changed in version" />;

export default VersionChanged;
