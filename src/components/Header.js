import React from 'react';
import Navbar from '../components/Navbar';

const Header = () => {
  return (
    <div>
      {/* TODO: Banner */}
      <Navbar />
    </div>
  );
};

export default Header;
