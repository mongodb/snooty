import React from 'react';
import Navbar from '../components/Navbar';

const Header = () => {
  return (
    <header>
      {/* TODO: Banner */}
      <Navbar />
    </header>
  );
};

export default Header;
