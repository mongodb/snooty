// import React, { useState } from 'react';

// const defaultContextValue = {
//   selectedQuizChoice: {},
//   setSelectedQuizChoice: () => {},
// };

// const QuizContext = React.createContext(defaultContextValue);

// const QuizProvider = ({ children }) => {
//    const [selectedQuizChoice, setSelectedQuizChoice] = useState({});

//    return <QuizContext.Provider value={{ selectedQuizChoice, setSelectedQuizChoice }}>{children}</QuizContext.Provider>;
// };

// export { QuizContext, QuizProvider};
